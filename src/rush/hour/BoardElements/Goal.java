package rush.hour.BoardElements;

import rush.hour.Tile;

import java.util.List;

public class Goal extends BoardElement {
    public Goal(char id, boolean moveable, List<Tile> tiles) {
        super(id, moveable, tiles);
    }
}
